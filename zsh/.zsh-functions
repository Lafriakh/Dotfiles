# Git: add, commit, push in one command.
function gitp() {
    git add .
    
    if [ "$1" != "" ]
    then
        git commit -m "$1"
    else
        git commit -m update
    fi
    
    git push
}
